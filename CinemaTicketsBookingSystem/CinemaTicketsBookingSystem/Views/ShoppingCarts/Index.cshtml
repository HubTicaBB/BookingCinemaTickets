@model CinemaTicketsBookingSystem.Models.ShoppingCart    

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}    

@if (Model == null)
{
    <div>
        <h3>Shopping Cart</h3>
        <p>Your shopping cart is empty</p>
        <div><a asp-controller="Showtimes" asp-action="Index" asp-route-id="@null" class="btn btn-outline-info">Check what's on today</a></div>
    </div>
}
else
{
    <form method="post">
        <br />
        <div class="card-header bg-dark text-light row">
            <div class="col-12 col-md-12">
                <div class="col-12">
                    <i class="fa fa-shopping-cart"></i> &nbsp; Shopping Cart
                    <a asp-controller="Showtimes" asp-action="Index" asp-route-id="@null" class="btn btn-outline-info btn-sm" style="position: absolute; right: 0">Continue Shopping</a>
                </div>
            </div>
        </div>
        <div class="card-body">
            <div class="rounded">
                @*@foreach (var item in Model.Items)
                {
                    <div class="row">
                        <div class="col-12 col-lg-3 offset-lg-1 text-center">
                            <img class="table-img rounded" src="@Url.Content(item.Showtime.Movie.PosterUrl)" />
                        </div>
                        <div class="col-8 col-lg-8">
                            <div class="row pl-2">
                                <h6 class="text-muted pb-2">Cinema Hall: <span class="text-light">@item.Showtime.CinemaHall.Name</span></h6>
                            </div>
                            <div class="row pl-2">
                                <h6 class="text-muted pb-2">Starting Time: <span class="text-light">@item.Showtime.Time.ToString("HH:mm")</span></h6>
                            </div>
                            <div class="row pl-2">
                                <h6 class="text-muted pb-2">Price: <span class="text-light">@item.Showtime.TicketPrice.ToString("C")</span></h6>
                            </div>
                            <div class="row pl-2">
                                <p class="text-muted" style="margin: 25px 0">@item.Showtime.Movie.Plot</p>
                            </div>
                            <div class="row pl-2">
                                <div class="col-2 text-primary"><h4>Count</h4></div>
                                <div class="col-2"><input class="form-control" asp-for="Count" /></div>
                            </div>
                        </div>

                    </div>
                }*@
            </div>
        </div>
    </form>
}

    @*<form method="post">


        <div class="card-body">
            <div class="rounded">
                @foreach (var item in Model.Items)
                {
                    <div class="row">
                        <div class="col-12 col-lg-3 offset-lg-1 text-center">
                            <img class="table-img rounded" src="@Url.Content(item.Showtime.Movie.PosterUrl)" />
                        </div>
                        <div class="col-8 col-lg-8">
                                <div class="row pl-2">
                                    <h6 class="text-muted pb-2">Cinema Hall: <span class="text-light">@Model.Showtime.CinemaHall.Name</span></h6>
                                </div>
                                <div class="row pl-2">
                                    <h6 class="text-muted pb-2">Starting Time: <span class="text-light">@Model.Showtime.Time.ToString("HH:mm")</span></h6>
                                </div>
                                <div class="row pl-2">
                                    <h6 class="text-muted pb-2">Price: <span class="text-light">@Model.Showtime.TicketPrice.ToString("C")</span></h6>
                                </div>
                                <div class="row pl-2">
                                    <p class="text-muted" style="margin: 25px 0">@Model.Showtime.Movie.Plot</p>
                                </div>
                                <div class="row pl-2">
                                    <div class="col-2 text-primary"><h4>Count</h4></div>
                                    <div class="col-2"><input class="form-control" asp-for="Count" /></div>
                                </div>
                            </div>

                    </div>
                }
            </div>
        </div>
        <div class="card-footer">
            <div class="row">
                <div class="col-12 col-md-6 pb-1">
                    <a asp-action="Index" class="btn btn-success form-control  btn-square btn-lg" style="height:50px;">Back to List</a>
                </div>
                <div class="col-12 col-md-6 pb-1">
                    <button type="submit" value="Add to Cart" class="btn btn-primary btn-square btn-lg form-control" style="height:50px;">Add to Cart</button>
                </div>
            </div>
        </div>
    </form>*@
